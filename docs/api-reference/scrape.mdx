---
title: "Web Scrape"
description: "Extract structured data or Markdown from any URL."
---

## Usage

使用 `uniskill_scrape` 将网页内容转换为 Agent 可直接理解的 Markdown 格式。

### Parameters

- `url` (string): 目标网页地址。
- `format` (string): 返回格式，可选 `markdown` (默认) 或 `html`。

```python
# Logic: Simple scraping example
# 逻辑：简易网页抓取示例

import requests

def scrape_page(target_url):
    api_url = "https://api.uniskill.ai/v1/scrape"
    # Variables in English, comments in Chinese
    # 变量全英文，注释用中文
    headers = {"Authorization": "Bearer YOUR_KEY"}
    payload = {"url": target_url, "format": "markdown"}
    
    response = requests.post(api_url, json=payload, headers=headers)
    return response.json()
```
